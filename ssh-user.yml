---
- hosts: "localhost"
  become: true
  tasks:
  - name: "Create user account"
    user:
      name: "user1"
      state: present
    
  - name: "Add authorized key"
    authorized_key:
      user: "user1"
      key: "user1_id_rsa.pub"
